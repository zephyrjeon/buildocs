/** @type {import('next').NextConfig} */
const nextConfig = {
  // https://nextjs.org/docs/pages/api-reference/next-config-js/basePath
  // set to deploy the app under "IP Address/buildocs" subdomain
  basePath: '/buildocs',
};

export default nextConfig;
